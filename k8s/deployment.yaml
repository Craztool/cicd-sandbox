apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sandbox-app
  template:
    metadata:
      labels:
        app: sandbox-app
    spec:
      containers:
      - name: web-server
        image: ghcr.io/craztool/cicd-sandbox:cc60ffd6e12d179b975609d74f72ac2687082ba0
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
